package dev.equerry.app

import org.junit.Assert.assertEquals
import org.junit.Test

/** Placeholder unit test so the JVM test task has something to run at Phase 0. */
class ExampleUnitTest {
    @Test
    fun sanity_addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
