<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Internet is required for the provider drivers (chat/vision/STT/TTS backends).
      No SMS / contacts / call-log permissions: side-effecting actions hand off to the
      system compose UIs via public intents (see spec §5, §8).
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!--
      Microphone for the voice round-trip (phase 05): the assist session captures the spoken
      question via system STT. Grantable from the in-session runtime prompt or from Equerry's
      settings; every denied state shows the same guidance (locked mic_permission decision).
    -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!--
      Camera for the "look through the camera and tell me what I'm looking at" capability: a single
      frame is captured, OCR'd, and sent to the VISION provider, then described aloud. Requested at
      first use; never recorded to storage (the frame is a transient request payload).
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <application
        android:name=".EquerryApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Equerry">

        <!--
          Assistant role (spec §10): the VoiceInteractionService + its session/recognition
          services let Equerry be set as the system default digital assistant. The launcher
          Activity below is the settings/onboarding entry point.
        -->
        <service
            android:name=".assistant.EquerryVoiceInteractionService"
            android:label="@string/assist_service_label"
            android:permission="android.permission.BIND_VOICE_INTERACTION"
            android:exported="true">
            <meta-data
                android:name="android.voice_interaction"
                android:resource="@xml/voice_interaction_service" />
            <intent-filter>
                <action android:name="android.service.voice.VoiceInteractionService" />
            </intent-filter>
        </service>

        <service
            android:name=".assistant.EquerryVoiceInteractionSessionService"
            android:permission="android.permission.BIND_VOICE_INTERACTION"
            android:exported="true" />

        <service
            android:name=".assistant.EquerryRecognitionService"
            android:permission="android.permission.BIND_RECOGNITION_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.speech.RecognitionService" />
            </intent-filter>
            <meta-data
                android:name="android.speech"
                android:resource="@xml/recognition_service" />
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Equerry">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
