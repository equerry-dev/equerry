/**
 * Provider Profiles and Capability-Slot routing. A Provider Profile is a saved backend
 * (type + baseUrl + key + model); a Capability Slot (CHAT/VISION/STT/TTS/OCR/EMBEDDINGS)
 * points at one profile. The slot system is the heart of the app (spec §3).
 */
package dev.equerry.app.providers
