/**
 * Assistant runtime: VoiceInteractionService / VoiceInteractionSession, the assistant
 * loop, and screen-context capture via the Assist API (AssistStructure + assist
 * screenshot). Never uses AccessibilityService (project rule r-01).
 */
package dev.equerry.app.assistant
