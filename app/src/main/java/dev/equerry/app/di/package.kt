/**
 * Hilt modules: bind driver implementations, the slot router, persistence, and the
 * networking stack (Retrofit + OkHttp with the key-redacting / auth-injecting
 * interceptors required by project rule r-03).
 */
package dev.equerry.app.di
