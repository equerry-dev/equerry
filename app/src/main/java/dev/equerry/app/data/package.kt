/**
 * Persistence: DataStore for non-secret settings; Keystore-backed
 * EncryptedSharedPreferences for API keys and endpoint credentials (project rule r-03).
 * Conversation history storage added (Room) only if it outgrows trivial.
 */
package dev.equerry.app.data
