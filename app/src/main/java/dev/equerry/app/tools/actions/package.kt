/**
 * Intent-based actions invoked by tools: set_timer, set_alarm, create_calendar_event,
 * draft_email, draft_message, open_app/launch_url, web_search, read_screen. All via public
 * intents only — no special permissions. Side-effecting actions stage for explicit user
 * confirmation (project rule r-02).
 */
package dev.equerry.app.tools.actions
